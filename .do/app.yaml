name: betchain
services:
- name: web
  source_dir: /
  github:
    branch: main  
    deploy_on_push: true
  run_command: cd app && python -m uvicorn main:app --host 0.0.0.0 --port $PORT
  environment_slug: python
  instance_count: 1
  instance_size_slug: basic-xxs
  routes:
  - path: /